# Ant Colony AI — Python dependencies
# O'rnatish:  pip install -r requirements.txt
# Python 3.10+ talab qilinadi.

# Web server va API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-multipart>=0.0.9

# LLM provayderlariga HTTP so'rovlar
aiohttp>=3.9,<4.0
httpx>=0.27,<1.0

# Provayder kalitlarini shifrlash (AES-256-GCM) — providers/secrets.py talab qiladi
cryptography>=41,<55
