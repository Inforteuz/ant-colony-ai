# docs/ — Specs and Status Notes

This folder holds project specs and runtime status notes for Ant Colony AI.

> Originally referenced from the top-level `README.md` directory tree
> (`docs/  # specs and status notes`). It was missing from the repo and has
> been created as a minimal placeholder so the README link resolves.

## Contents

- [status.md](status.md) — current provider integrations and known issues.

## Conventions

- Keep notes here short and factual.
- For longer design docs, add a file and link it from this index.
- Runtime state (keys, caches, logs) lives in `data/` — **not** here.
